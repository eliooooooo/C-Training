Faire une fonction de tri générique qui peut prendre n'importe quelle donnée en entrée (y compris des structures)
la fonction prend trois parametres: 
    <void*> array
    <int> length
    <int> pointeur vers une fonction générique